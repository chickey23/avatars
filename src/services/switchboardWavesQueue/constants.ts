/** Rise-into-slot duration for new queue items (ms); slower = clearer motion. */
export const SWITCHBOARD_WAVE_TRAVEL_MS = 4_000;

/** Hide Waves column entirely at or below this viewport width. */
export const WAVES_COLUMN_HIDE_MAX_WIDTH_PX = 420;

/** Below this width, skip long travel; blink at bottom then jump when settled. */
export const WAVES_BLINK_ONLY_MAX_WIDTH_PX = 900;
