export const LEGACY_POPULARITY_STORAGE_KEY = "avatars_avatar_popularity_v1";

/** Default roster priority when unset (0–100). */
export const DEFAULT_ROSTER_SCORE = 50;

export const MIN_ROSTER_SCORE = 0;
export const MAX_ROSTER_SCORE = 100;

/** Same cap as legacy routing bias magnitude (see former avatarPopularity). */
export const ROUTING_BIAS_CAP = 22;
