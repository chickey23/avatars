# Handoff — 2026-04-12

## TL;DR (next session)

1. **Regression:** run **[docs/TEST_PLAN.md](docs/TEST_PLAN.md)** (`npm run verify` + targeted manual rows). The full **plan → use → reflect** loop and **Cursor** skill/rule pointers are in **[docs/DEVELOPMENT_CYCLE.md](docs/DEVELOPMENT_CYCLE.md)**; `TEST_PLAN` stays the detailed matrix. Also **[Verification checklist](#verification-checklist)** (Ollama, WoS, cascade, queued send, session log, **Chat Visualizer / Waves**, **Storage viz**). Optionally spot-check **proactive** sidebar: new mail → pending line + badge list + prompt block when Ollama is up. If you always launch via [`start-dev.cmd`](start-dev.cmd), **`verify` already runs** on startup unless `SKIP_VERIFY=1` — a separate `npm run verify` is still recommended **before merge** or after **non-UI** changes; for fast pure-UI iteration you may rely on the launch verify only.
2. **Work:** follow **[SPEC.md](SPEC.md) § Implementation Order (Active)** — **world model / shared metadata** first ([`docs/WORLD_MODEL_AND_PREPROCESSOR.md`](docs/WORLD_MODEL_AND_PREPROCESSOR.md)): Projects hub, `data/metadata/` path, UI / chat entry, bridge **`Avatar.assignedTasks`** and execution context. **Conversation archive** segments/chapters as follow-on. **Proactive:** timer/cue when prioritized; **sequential multi-avatar release** polish is **lower priority** (MVP acceptable). **Active Task** / **Focus Watcher**; **additional connectors** per SPEC (reference wikis/social listed as deferred supplemental sources). **In parallel (operability):** (a) per–view–style / per-window–style **user chrome** color; (b) **Clear chat** vs **End topic** (single composed action vs clearer dual controls); (c) **Waves** — tool name, error codes, and non-secret **arg** visibility for denied/failed tools ([docs/AGENTIC_TOOLS.md](docs/AGENTIC_TOOLS.md), `allowedAgenticToolIds`); (d) **Audio + visual** — every primary cue pairs sound with a visible pulse (see [PROGRESS.md](PROGRESS.md) TL;DR operability).
3. **Dev entry:** [`start-dev.cmd`](start-dev.cmd) runs [`scripts/verify.ps1`](scripts/verify.ps1) unless `SKIP_VERIFY=1` (set `SKIP_VERIFY=1` to skip verify on that launch for faster startup — still run `npm run verify` before merge if you skipped). [`build-release.cmd`](build-release.cmd) = `tauri build` only. Stamps under [`.local/`](.local/) (gitignored).
4. **Detail:** **[PROGRESS.md](PROGRESS.md)** — status; **[docs/IMPLEMENTATION_ROADMAP.md](docs/IMPLEMENTATION_ROADMAP.md)** — phased roadmap (bench + popularity ordering); **[TECHSPEC.md](TECHSPEC.md) § 2** — file tree.

**Terminology:** **[docs/STYLEGUIDE.md](docs/STYLEGUIDE.md)**. **Older handoff:** **[HANDOFF.md](HANDOFF.md)**.

---

## Completed (repo state — includes prior sessions)

- **Chat Visualizer (Waves):** Persistent queue ([`switchboardWavesQueue/`](src/services/switchboardWavesQueue/)), [`SwitchboardViz.tsx`](src/components/SwitchboardViz.tsx), `onTraceProgress` + **`onWaveChatComplete`** → [`markWaveSettledForUserDepth`](src/services/switchboardWavesQueue/operations.ts); turn **`finally`** → [`markWavesSettledForUser`](src/services/switchboardWavesQueue/operations.ts). Docs: [`docs/SWITCHBOARD_VISUALIZATION.md`](docs/SWITCHBOARD_VISUALIZATION.md).
- **Storage visualizer (right column):** [`SourceCacheViz.tsx`](src/components/SourceCacheViz.tsx) — optional toolbar **Storage viz**; resizable width `avatars_source_cache_viz_width_px`; shows Gmail insight cache stats, **in-prompt vs below-top-K** inbox rows (`lastEmailRankingDiagnostics` on [`SituationContext`](src/types/index.ts), set in [`processUserTurn`](src/store/appStore.ts) via [`rankEmailsLinesAndDiagnostics`](src/services/contextScoring/email.ts); stripped by [`stripEphemeralFields`](src/store/appStore.ts)), world-metadata counts, worldview **audit tail**, waves persist key, **Background** monitor-contract rows + **contract:** session-log tail (see completed bullets below); **structured hover/click panel** (not native `title`). Phase A fallback: [`parseRankedEmailLinesFromRelevantData`](src/services/sourceCacheViz/relevantDataEmailLines.ts). Planned connectors (Reddit, Hotmail, YouTube, Steam) as stub list in UI.
- **SPEC:** § **Proactive notifications and pending reactions**; Situation Context + Implementation Order cross-links; § Implemented UI (clear chat, proactive sidebar, **Waves**); roadmap prioritizes **metadata/projects**; **sequential release batch** deprioritized.
- **User-turn context scoring (MVP):** Email, **calendar**, and **contacts** — [`scoreAndFormatEmails`](src/services/contextScoring/email.ts), [`scoreAndFormatCalendarEvents`](src/services/contextScoring/calendar.ts), [`scoreAndFormatContacts`](src/services/contextScoring/contacts.ts), merged in [`processUserTurn`](src/store/appStore.ts) (`relevantData` assembly; same pattern as email). Docs: [`docs/CONTEXT_SCORING.md`](docs/CONTEXT_SCORING.md), per-source docs under [`docs/`](docs/).
- **World metadata v1:** [`src/services/worldMetadata/`](src/services/worldMetadata/) — JSON in `localStorage`, debounced persist, contact scoring overlay (see PROGRESS / CONTEXT_SCORING docs).
- **Proactive pipeline (MVP):** [`pendingNotifications.ts`](src/services/pendingNotifications.ts) — per-avatar scores for new emails (cap 3 avatars, top-K new mail per eval), `mergeProactiveEvaluation`, thread revision, `computeReleasedClusterIds`; [`processUserTurn`](src/store/appStore.ts) + [`AppProvider`](src/context/AppProvider.tsx) interval refresh; [`avatarAgents.ts`](src/services/avatarAgents.ts) pending block in Ollama prompt + `pendingNotificationsBlock` on debug.
- **Types / persistence:** `PendingNotification`, `userFocus`, `pendingReleaseClusterIds` (ephemeral), `proactiveProcessedEmailIds`, [`writePersistedContext`](src/store/appStore.ts), [`stripEphemeralFields`](src/store/appStore.ts).
- **Avatar sidebar UI:** Card is not a single button — **select** (name), **badge** (expand pending list), **🔍** (description + traits); **truncated** first pending line; **short** high-urgency strip above chat (sidebar pointer). **Discuss** / **Dismiss**; **Clear chat** — **no** `window.confirm` ([`AppProvider.tsx`](src/context/AppProvider.tsx)).
- **Dev scripts:** [`scripts/verify.ps1`](scripts/verify.ps1), [`start-dev.cmd`](start-dev.cmd), [`build-release.cmd`](build-release.cmd), [`npm run verify`](package.json), `.local/` in `.gitignore`.
- **Tests:** [`email.test.ts`](src/services/contextScoring/email.test.ts) (includes `rankEmailsLinesAndDiagnostics`), [`relevantDataEmailLines.test.ts`](src/services/sourceCacheViz/relevantDataEmailLines.test.ts), [`calendar.test.ts`](src/services/contextScoring/calendar.test.ts), [`contacts.test.ts`](src/services/contextScoring/contacts.test.ts), [`pendingNotifications.test.ts`](src/services/pendingNotifications.test.ts), [`operations.test.ts`](src/services/switchboardWavesQueue/operations.test.ts) (waves queue).
- **Structured tools (worldview):** `avatars_tools_v1` parse in [`worldviewTools/parse.ts`](src/services/worldviewTools/parse.ts) (preprocess / flat envelope / smart quotes); execute in [`execute.ts`](src/services/worldviewTools/execute.ts); **malformed-output** heuristics + session log warn in [`diagnose.ts`](src/services/worldviewTools/diagnose.ts); **prompt** — separate **Guidelines** vs **Tool protocol** in [`avatarAgents.ts`](src/services/avatarAgents.ts), profile text in [`agenticTools/toolProtocol.ts`](src/services/agenticTools/toolProtocol.ts), intent [`turnToolIntent.ts`](src/services/turnToolIntent.ts), transcript scrub [`modelTranscript.ts`](src/services/modelTranscript.ts), optional **repair** Ollama pass + logs `ollama_tool_parse_repair`; **golden fixtures** [`worldviewTools/__fixtures__/modelReplies/`](src/services/worldviewTools/__fixtures__/modelReplies/). **Model reply / tools parse** under expanded Ollama prompt in [`App.tsx`](src/App.tsx); **Waves** orange **!** + `parse: malformed` row via [`onWorldviewParseDiagnostic`](src/services/switchboard.ts) → [`AppProvider.tsx`](src/context/AppProvider.tsx). **Tool Workshop** intent rollups: [`toolTelemetry/store.ts`](src/services/toolTelemetry/store.ts).
- **Gmail fetch allowlist (user turn):** All loaded inbox ids for the turn (not only top-K) — [`processUserTurn`](src/store/appStore.ts); still enforced in [`gmailFetchTools.ts`](src/services/gmailFetchTools.ts).
- **Relevant context for Ollama:** Ranked `email [id …]` lines are not dropped by the 25-line cap — [`relevantContextPrompt.ts`](src/services/relevantContextPrompt.ts).
- **Project → avatar task:** Successful `world_metadata.patch_projects` calls [`ensureProjectTaskForAvatar`](src/services/projectAvatarLink.ts) → long-term task + `avatars:assigned-task` → sidebar **assignedTasks** merge in [`App.tsx`](src/App.tsx).
- **Routing feedback:** **Unhelpful reply** on avatar bubbles → [`avatarPopularity.ts`](src/services/avatarPopularity.ts) biases [`switchboard.ts`](src/services/switchboard.ts) literal + semantic scores.
- **WV audit:** Rows store **revertible** patch payloads; **Revert bad patches** in WV log + [`worldviewRevert.ts`](src/services/worldviewRevert.ts); focus cleared if reverted project/contact id matches.
- **Broadcast / single-wave routing:** [`processUserTurn`](src/store/appStore.ts) sets `switchboardRoutingMode: "single_wave"` and passes [`routingMode: "single_wave"`](src/services/switchboard.ts) — no opinion cascade; session log `switchboard_single_wave` with would-have-cascaded ids; hidden replies via **`AVATARS_NO_COMMENT`** or optional preflight skip when `preflightOllamaMinScore` is set (`avatar_reply_suppressed`, `preflight_skip`).
- **Agentic tools registry:** [`docs/AGENTIC_TOOLS.md`](docs/AGENTIC_TOOLS.md), [`agenticTools/`](src/services/agenticTools/) — `registry.ts` (ids + `avatarMayUseAgenticTool`; empty `allowedAgenticToolIds: []` opts out of non-group tools), `toolProtocol.ts` (per-profile prompt blocks), JSON + lexical merge, permission checks in [`execute.ts`](src/services/worldviewTools/execute.ts) / [`gmailFetchTools.ts`](src/services/gmailFetchTools.ts).
- **Monitor registry & synthetic chat:** [`services/monitors/`](src/services/monitors/) — `MonitorDef` contracts, claimants via `monitor:<name>` on [`Avatar.systemTags`](src/types/index.ts); [`postSynthetic.ts`](src/services/monitors/postSynthetic.ts) appends monitor-authored lines to the thread (tag chip, **Unhelpful** dismissal); **Waves** queue type `monitor_prompt` ([`switchboardWavesQueue/types.ts`](src/services/switchboardWavesQueue/types.ts)); meta-monitor for unclaimed/duplicate contracts; **Unassigned Project Manager** + project affinity suggestions.
- **Background observability:** [`contractLog`](src/services/sessionLog/contractLog.ts) — session-log categories `contract:<contract>__<event>` for source runners / scheduler (structured contract events); **Storage viz** [`SourceCacheViz.tsx`](src/components/SourceCacheViz.tsx) **Background** panel lists monitor contracts with claimant avatars and tails `contract:` plus `platform_*` log lines from [`platformLog`](src/services/platform/platformLog.ts) (prefix from [`PLATFORM_LOG_CATEGORY`](src/services/platform/constants.ts)). Core: [`services/platform/`](src/services/platform/); Tauri [`platform_cache_*`](src-tauri/src/platform_cache.rs) and `%LOCALAPPDATA%/.../data/platform/`. See [`docs/PLATFORM_PERSISTENCE.md`](docs/PLATFORM_PERSISTENCE.md).
- **Agentic draft tools:** `drafts.tasks` / `drafts.calendar_event` / `drafts.email_reply` in [`agenticTools/registry.ts`](src/services/agenticTools/registry.ts), executed in [`worldviewTools/execute.ts`](src/services/worldviewTools/execute.ts). Durable rows: [`platform/drafts.ts`](src/services/platform/drafts.ts).
- **Project hygiene:** Seed list [`projectSeedList.ts`](src/data/projectSeedList.ts), placeholder-title rejection [`titleSanity.ts`](src/services/worldMetadata/titleSanity.ts), startup prune/seed order in [`AppProvider.tsx`](src/context/AppProvider.tsx).
- **Steward avatars (defaults):** Inbox / calendar / contacts / timekeeper roles in [`defaultAvatars.ts`](src/data/defaultAvatars.ts) holding `monitor:source_runner:*` or `monitor:due_and_snoozed_items` tags as applicable — see catalog, not duplicated here.

---

## Next priorities

1. **World model / shared metadata** — [`docs/WORLD_MODEL_AND_PREPROCESSOR.md`](docs/WORLD_MODEL_AND_PREPROCESSOR.md); SPEC [`data/metadata/`](SPEC.md) (People, Events, **Projects**); **UI** / chat entry; bridge **`assignedTasks`** and execution-oriented context — **world metadata v1** is a stepping stone in `localStorage`; on-disk / Tauri path TBD.
2. **Conversation archive** — **Archive segment / dismiss topic** and project-linked chapters (follow-on to metadata/todos).
3. **Background agents** — **Active Task** and **Focus Watcher**; extend **context scoring** as new connectors ship.
4. **Proactive (ongoing)** — Timer/cue; sequential batch **polish** when prioritized (not blocking).
5. **Additional sources** — Reference/social supplemental connectors listed in SPEC § Data Sources (deferred); Hotmail, Weather API, etc.
6. **Tests** — expand coverage (Switchboard, connectors). **Signature phrase** — continue SPEC § Behavioral Instructions (workflow in active use).
7. **Operability (parallel)** — does not replace items 1–6; ship as bandwidth allows, often alongside shell work. **(a)** **User chrome color** — persist per **view mode** and/or **chat window style** (keyed storage; see `avatars_user_chrome_color` in [`appChromeConstants.ts`](src/app/appChromeConstants.ts)). **(b)** **Clear chat** vs **End topic** — product decision: optional single control that runs both, or keep both with copy that points to [PROGRESS.md](PROGRESS.md) / SPEC archive. **(c)** **Waves / tool denials** — show **which tool** failed, **error** (`permission_denied`, `permission_denied_projects`, …), and **non-secret arg preview** in-column or in an expandable row; **policy** fix remains expanding **`allowedAgenticToolIds`** / prompts per [docs/AGENTIC_TOOLS.md](docs/AGENTIC_TOOLS.md). **(d)** **Audio + visual** — `emitAudioVisualCue` for primary cues; document exceptions (e.g. `focusMode`, reduced motion). Manual checks: [docs/TEST_PLAN.md](docs/TEST_PLAN.md) A11–A12.

---

## Deferred (consult user before building)

- **Switchboard / cue sound:** keep **non-critical** or decorative sounds secondary to visualization and text; **primary** user-facing cues should still pair **audible** feedback with a **visible** pulse (or a documented, accessible alternative). Accessibility controls (including reduced motion) if expanded.
- **Archive segment, log cap, compression:** SPEC § Conversation archive; PROGRESS § Deferred; TECHSPEC § 12.7.

---

## Verification checklist

- [ ] **Tri-state:** Ollama stopped vs listening with no models vs ready — badge and Rules/Fallback messaging make sense.
- [ ] **WoS:** With “Use in chat context”, WoS text appears in **Full prompt** / relevant-data path.
- [ ] **Cascade:** Two avatars in sequence — second avatar sees prior avatar as context (thread tail).
- [ ] **Send while pending:** Queue more user messages while replies stream; order and anchors stay sane.
- [ ] **Waves:** Toggle column; dots rise; earlier wave stops blinking when that wave’s replies appear; later cascade wave can still blink until its replies show.
- [ ] **Storage viz:** Toggle **Storage viz**; resize handle between transcript and column; hover/click chips — **Inbox** shows top-K lines and **Scored, not in prompt** when fetch batch is larger than top-K; **Gmail** shows insight relevance counts; **Open Worldview tab** switches context tab.
- [ ] **Context → Internet (optional):** With Tauri desktop, open **Internet** tab; adjust **Context depth**; run a query; add selected hits; confirm pinned count; send a user turn and confirm prompt / avatars see `Internet context` lines (see [docs/TARGETED_SEARCH.md](docs/TARGETED_SEARCH.md)).
- [ ] **Context scoring (optional):** With Gmail data loaded, email / calendar / contacts ranked lines appear in the relevant-data path where applicable.
- [ ] **Session log:** New entries and (in Tauri) files under `%LOCALAPPDATA%\…\avatars\session_logs\` (or equivalent).
- [ ] **Verify script:** `npm run verify` — `npm install` when lock changes, `vitest`, `verify ok` in console; `.local/verify.log` append.
- [ ] **Worldview parse diagnostics:** Expand **Model reply / tools parse** on an Ollama message; session log shows `worldview_tools_parse_mismatch` when model emits bad tool prose; **Waves** shows **!** for parse warnings when visualizer is on.
- [ ] **Project from tools:** Avatar creates project via valid JSON tools → project in **Projects** tab + task on avatar + assign dropdown refreshes.
- [ ] **WV revert:** Use **Revert bad patches** on a row that applied metadata; data rolls back; reverted row marked in log.
- [ ] **Single-wave / no-comment:** With multiple avatars selected, only one routing wave runs (no cascade); session log may contain `switchboard_single_wave`. A suppressed reply logs `avatar_reply_suppressed` / `preflight_skip`.
- [ ] **Lexical tools (optional):** In Ollama reply, use `AVATARS_MEM: note` and/or `AVATARS_TOOL name=gmail.fetch_message_body messageId=…` — confirm profile/Gmail behavior matches [AGENTIC_TOOLS.md](docs/AGENTIC_TOOLS.md).
- [ ] **Waves — denied or failed tool (optional):** When an avatar returns tools but execution fails (e.g. `permission_denied`), confirm you can see **which tool** and **why** (tooltip, WV log, or **Model reply / tools parse**); after [PROGRESS](PROGRESS.md) operability work, confirm in-column or expanded detail + non-secret arg preview. See [docs/TEST_PLAN.md](docs/TEST_PLAN.md) A11.
- [ ] **Audio + visual (optional):** Trigger a cue that should play a sound; a **visible** pulse or fallback affordance should appear (unless `focusMode` / reduced motion / known exception). See [docs/TEST_PLAN.md](docs/TEST_PLAN.md) A12.
